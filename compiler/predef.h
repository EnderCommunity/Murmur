void writeLogLine(char *title, int type, char *message, int lines, int line, int col);

char *getDat(char typ, int id, int goBck);

int ENVI_ENABLE_COMMENTS = 1,
    ENVI_CHECK_FOR_PREPROCESSOR_METHODS = 1,
    ENVI_ENABLE_DEBUG_MODE = 0,
    ENVI_ENABLE_IMPORT_METHOD = 1,
    ENVI_ENABLE_USING_METHOD = 1;
